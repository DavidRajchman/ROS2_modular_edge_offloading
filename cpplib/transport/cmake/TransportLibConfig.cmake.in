@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
# If your library has public dependencies that consumers need to find, add them here.
# For example: find_dependency(Threads REQUIRED)

if(NOT TARGET TransportLib::transport)
    include("${CMAKE_CURRENT_LIST_DIR}/TransportLibTargets.cmake")
endif()

check_required_components(TransportLib)